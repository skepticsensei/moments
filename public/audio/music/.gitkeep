# Place ambient music files here.
# File names must match the `src` paths in src/data/music.ts.
# Example: /audio/music/soft-river.mp3
#
# Tracks loop automatically if shorter than the meditation.
# Default playback volume is 0.35 (voice = 1.0).
